pipeline {
    agent {projekt3 'projekt3'}
    stages { 
        stage('build') {
            steps {
               sh "ls -la"
            }
        }
        stage('deploy') {
            steps {
                sh "pwd"
            }
        }
    }
}
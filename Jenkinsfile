pipeline {
    agent {label 'master'}
    stages { 
        stage('build') {
            steps {
               sh "ls -la"
                sh "Hello!!!)))"
                            }
        }
                stage('deploy') {
            steps {
                sh "pwd"
                sh "Good buy ((("
            }
        }
    }
}

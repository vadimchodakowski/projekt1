pipeline {
    agent {label 'projekt4'}
    stages { 
        stage('build') {
            steps {
                sh 'docker build'
               sh "ls -la"
                sh "Hello!!!)))"
                            }
        }
                stage('deploy') {
            steps {
                sh "pwd"
                sh "Good buy ((("
            }
        }
    }
}

pipeline {
    agent {label 'jenkins'}
    stages { 
        stage('build') {
            steps {
               sh "ls -la"
            }
        }
                stage('deploy') {
            steps {
                sh "pwd"
            }
        }
    }
}
pipeline {
    agent any
    stages { 
        stage('build') {
            steps {
               sh "ls -la"
            }
        }
        stage('deploy') {
            steps {
                sh "pwd"
            }
        }
    }
}
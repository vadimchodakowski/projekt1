pipeline {
    agent {label 'jenkins'}
    stages { 
        stage('build') {
            steps {
               sh "ls -la"
                sh "Hello!!!)))"
                triggers { pollSCM('*****') }
            }
        }
                stage('deploy') {
            steps {
                sh "pwd"
                sh "Good buy ((("
            }
        }
    }
}

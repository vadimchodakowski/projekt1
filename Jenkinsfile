pipeline {
    agent {label 'master'}
    stages { 
        stage('build') {
            steps {
               sh "ls -la"
                sh echo "Hello!!!)))"
                            }
        }
                stage('deploy') {
            steps {
                sh "pwd"
                sh echo "Good buy ((("
            }
        }
    }
}

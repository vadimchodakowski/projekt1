pipeline {
    agent {label 'projekt3'}
    stages { 
        stage('build') {
            steps {
               sh "ls -la"
            }
        }
        stage('deploy') {
            steps {
                sh "pwd"
            }
        }
    }
}
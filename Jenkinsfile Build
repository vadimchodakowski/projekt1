pipeline {
    agent {label 'projekt3'}
    stages { 
        stage('build') {
            steps {
                sh "echo BUILD"
                build job: = '<Deploy Petclinic>' , propagate: true, wait: true
            }
        }
        
    }
}

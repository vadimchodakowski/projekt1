pipeline {
    agent {label 'projekt3'}
    stages { 
        stage('build') {
            steps {
                sh "echo BUILD"
                sh "Ls -la"
                currentBuild.displayName = "$Deploy Petclinic"
            }
        }
        
    }
}
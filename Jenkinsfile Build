pipeline {
    agent {label 'projekt3'}
    stages { 
        stage('build') {
            steps {
                sh "echo BUILD"
                def job = build job: 'Deploy Petclinic' 
            }
        }
        
    }
}

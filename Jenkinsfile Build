pipeline {
    agent {label 'projekt3'}
    stages { 
        stage('build') {
            steps {
                sh "echo BUILD"
                sh "ls -la"
            }
        }
        
    }
}
